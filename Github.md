#Github Terminal Tricks
- installation
  ```
  sudo apt install gh
  ```
- authentication
  ```
  gh auth login
  ```
- see your repos
  ```
  gh repo list
  ```
- clone repo
  ```
  gh repo clone <username>/reponame
  ```
